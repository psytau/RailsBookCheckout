$('#star').raty({
    readOnly: true,
    start: <%= @book.rating %>,
    path: '/assets'
});

$('#user_star').raty({
    start: <%= @rating.score %>,
    path: '/assets',
    click: function(score, evt) {
        $.ajax({
            url: '/ratings/' + <%= @rating.id %>,
            type: 'UPDATE',
            data: { score: score }
            success: function() {
                $('#user_start').html(data);
            }
        }).done(function (data){
        });
    }
});

